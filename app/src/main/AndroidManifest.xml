<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ──────────────── Permissions ──────────────── -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- SMS: required to read and receive bank/UPI payment SMS -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Boot: allows SyncWorker to be re-scheduled after device restart -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".TrackItApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrackIt">

        <!-- ──────────────── Main Activity ──────────────── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TrackIt">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Link Support -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="trackit" />
            </intent-filter>
        </activity>

        <!-- ──────────────── SMS Receiver ──────────────── -->
        <!--
            android:exported="true"  — required; SMS_RECEIVED is a system broadcast.
            android.permission.BROADCAST_SMS — prevents third-party apps from spoofing
            SMS intents to this receiver.

            Priority 999 on SMS_RECEIVED: processes bank SMSs before other apps read
            the message (does NOT abort the broadcast — it only prioritises our handler).
        -->
        <receiver
            android:name=".sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">

            <!-- Intercept incoming SMS from the system -->
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>

            <!-- Re-enqueue WorkManager sync after device reboot -->
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- ↑ QUICKBOOT_POWERON covers HTC/OnePlus fast-boot (cold start) -->
            </intent-filter>

        </receiver>


        <!-- ──────────────── Widget Receiver ──────────────── -->
        <!--
            TrackItWidgetReceiver drives the Glance home screen widget.
            android:exported="true" is required — the system sends APPWIDGET_UPDATE
            broadcasts from outside the app's process.
        -->
        <receiver
            android:name=".widget.TrackItWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/trackit_widget_info" />
        </receiver>

        <!--
            FileProvider for sharing CSV exports via ACTION_SEND.
            Authority must match the string used in ExportHelper.kt.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
            Disable WorkManager's default auto-initializer so that TrackItApp
            can supply a custom Configuration.Provider with HiltWorkerFactory.
            Without this, WorkManager initialises before Hilt is ready and the
            app crashes at the first @HiltWorker injection.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
